package finalProject.app.fcm.util;

import android.view.View;

public interface OnParkingLotItemClickListener {
    void onItemClick(MainRecycleViewAdapter.ViewHolder holder, View view, int position);
}
